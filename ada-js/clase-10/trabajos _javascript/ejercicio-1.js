const prompt = require('prompt-sync')();
// Declaramos dos variables usando la palabra reservada "let".
let edad = 25;  // Asignamos un valor representando la edad.
let peso = 70;  // Asignamos un valor representando el peso.

console.log(edad, peso);  // Mostramos los valores de las variables.