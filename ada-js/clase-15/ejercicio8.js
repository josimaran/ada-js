// Ejercicio 8: Extraer una parte de una frase

// Frase original
let frase = "Los gatos son geniales";

// Extraemos la palabra "gatos" usando el método slice
let parteExtraida = frase.slice(4, 9); // Extraemos de la posición 4 a la 9


console.log(parteExtraida);

